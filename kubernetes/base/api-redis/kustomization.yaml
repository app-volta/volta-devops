apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Peças que compõem a api-redis (ranqueamento de cooperativas). O que varia por ambiente mora em overlays/.
resources:
  - deployment.yaml
  - service.yaml
