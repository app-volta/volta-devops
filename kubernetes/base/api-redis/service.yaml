apiVersion: v1
kind: Service
metadata:
  name: api-redis
  labels:
    app: api-redis
spec:
  type: ClusterIP
  selector:
    app: api-redis
  ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP
