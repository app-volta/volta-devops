apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Conjunto completo da aplicação, sem nada específico de ambiente.
# QA e produção partem daqui e só declaram as diferenças (veja overlays/).
resources:
  - api
  - chatbot
  - ingress.yaml
